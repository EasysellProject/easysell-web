import { CSSProperties } from "react";

export default {
    container: {
        backgroundColor: 'white',
        display: 'flex',
        flexDirection: 'column',
        flex: 1,
    } as CSSProperties,
}