import { CSSProperties } from "react";
import { APP_COLORS } from "../../styles";

export default {
    headerText: {
        color: APP_COLORS.textGreen,
        fontWeight: 'bold'
    } as CSSProperties
}