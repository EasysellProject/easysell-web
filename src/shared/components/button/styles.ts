import { CSSProperties } from "react";

export default {
    container: {
        borderWidth: 0,
        backgroundColor: 'transparent'
    },
    spinner: {
        flex: 1,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    } as CSSProperties
}