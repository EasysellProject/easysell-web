import { CSSProperties } from "@material-ui/core/styles/withStyles"

const flexColum: CSSProperties = {
    display: 'flex',
    flexDirection: 'column'
}

const flexRow: CSSProperties = {
    display: 'flex',
    flexDirection: 'row'
}

export default { flexColum, flexRow }